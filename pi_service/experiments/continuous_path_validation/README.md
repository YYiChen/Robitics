# 连续路径前视循迹（独立验证）

适用于连续的黑色或白色胶带路径，不假设矩形、八边形、固定转弯次数或固定转向方向。可用于六边形、七边形、八边形和由多段直线组成的连续折线路线。

每帧从紫色骨架路线取得 `LOOKAHEAD` 前视点，再用差速 P 控制向该点连续转向。默认安全行为：启动看不到线即停车；连续三帧找不到线则停车。先以显示模式验证，再添加 `--enable-motors` 实车低速测试。

浏览器预览端口为 `5054`。
